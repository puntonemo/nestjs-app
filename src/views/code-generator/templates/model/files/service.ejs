import { Injectable } from '@nestjs/common';
import { User } from '@model/user/user.model';
import { <%-model_name%>sRepository, Create<%-model_name%>Dto, Find<%-model_name%>Dto, Update<%-model_name%>Dto } from '@model/<%-model_name.toLowerCase()%>/<%-model_name.toLowerCase()%>.model';

@Injectable()
export class <%-model_name%>sService {
    constructor(private readonly <%-model_name.toLowerCase()%>sRepository: <%-model_name%>sRepository) {}
    create(create<%-model_name%>Dto: Create<%-model_name%>Dto, user: User) {
        return this.<%-model_name.toLowerCase()%>sRepository.create(create<%-model_name%>Dto, user);
    }

    findAll(filters: Find<%-model_name%>Dto) {
        return this.<%-model_name.toLowerCase()%>sRepository.findAll(filters);
    }

    findOne(id: number) {
        return this.<%-model_name.toLowerCase()%>sRepository.findOne(id);
    }

    update(id: number, update<%-model_name%>Dto: Update<%-model_name%>Dto, user: User) {
        return this.<%-model_name.toLowerCase()%>sRepository.update(id, update<%-model_name%>Dto, user);
    }

    remove(id: number) {
        return this.<%-model_name.toLowerCase()%>sRepository.remove(id);
    }
}
